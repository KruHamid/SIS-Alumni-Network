import { AlumniProfile, BusinessCategory } from './types';

// This file is now empty as data is fetched from the Google Sheet.
// It is kept to avoid breaking existing imports, but it can be removed if all imports are updated.
